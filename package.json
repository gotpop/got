{
    "devDependencies": {
        "netlify-cms": "^2.10.129",
        "parcel": "next"
    },
    "name": "got",
    "version": "0.1.0",
    "scripts": {
        "prebuild:dev": "rm -rf static/admin",
        "build:dev": "yarn parcel watch assets/cms/index.html --dist-dir static/admin --public-url ./",
        "build:prod": "yarn parcel build assets/cms/index.html --dist-dir static/admin --public-url ./"
    }
}