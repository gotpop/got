import homePageContent from "./homePageContent";

const home = {
    label: "Home",
    name: "home",
    description: "This is the homepage config",
    files: [
        homePageContent
    ]
};

export default home;