const image = {
    label: "Add Image",
    name: "thumbnail",
    widget: "image",
    default: "/uploads/code.jpg",
    hint: "Add a primary image the card here",
    media_library: {
        config: {
            multiple: true
        }
    }
};

export default image;
